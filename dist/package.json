{
  "name": "taskschedulepro",
  "version": "1.0.0",
  "description": "TaskSchedulePro - Professional Task Management System",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "minify:js": "esbuild script.js --bundle --minify --target=es2020 --outfile=dist/script.min.js",
    "minify:css": "esbuild styles.css --minify --outfile=dist/styles.min.css"
  },
  "dependencies": {
    "esbuild": "^0.15.0"
  },
  "type": "module"
}
